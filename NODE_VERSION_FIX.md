# ✅ Исправление ошибки Node.js Version

## 🔴 Ошибка, которую вы видели:

```
Error: Unable to find Node version '20' for platform linux and architecture x64.
```

---

## 🎯 Что было исправлено:

### ❌ **Было (неправильно):**

```yaml
- name: Setup Node
  uses: actions/setup-node@v4
  with:
    node-version: '20'  # ❌ Слишком общая версия
```

### ✅ **Стало (правильно):**

```yaml
- name: 🟢 Setup Node.js
  uses: actions/setup-node@v4
  with:
    node-version: '20.x'  # ✅ Указываем любую версию из 20.x линейки
```

---

## 🔍 Причина проблемы:

GitHub Actions требует более конкретную спецификацию версии:

| Формат | Работает? | Пример |
|--------|-----------|---------|
| `'20'` | ❌ Нет | Слишком общо |
| `'20.x'` | ✅ Да | Любая версия 20.x |
| `'20.10.0'` | ✅ Да | Конкретная версия |
| `'lts/*'` | ✅ Да | Последняя LTS |

---

## ✅ Что еще было исправлено:

### 1️⃣ **Добавлены Environment Variables**

**Проблема:** Build падал бы на следующем шаге из-за отсутствия Supabase ключей.

**Решение:** Добавлена секция `env:` в build шаг:

```yaml
- name: 🏗️ Build with environment variables
  run: npm run build
  env:
    VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
    VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
    # ... остальные переменные
```

---

### 2️⃣ **Файл перемещен в правильное место**

**Было:**
```
/workflows/deploy.yml  ❌ (неправильно)
```

**Стало:**
```
/.github/workflows/deploy.yml  ✅ (правильно)
```

GitHub Actions ищет workflows **ТОЛЬКО** в папке `.github/workflows/`!

---

## 🚀 Что делать СЕЙЧАС:

### 1️⃣ Push исправленный файл:

```bash
# Добавьте исправленный workflow
git add .github/workflows/deploy.yml

# Commit
git commit -m "🔧 Fix Node.js version + add env variables"

# Push
git push origin main
```

---

### 2️⃣ Убедитесь что Secrets добавлены:

**Откройте:**
```
https://github.com/YOUR_USERNAME/YOUR_REPO/settings/secrets/actions
```

**Должны быть добавлены (минимум):**
- ✅ `VITE_SUPABASE_URL`
- ✅ `VITE_SUPABASE_ANON_KEY`

**Где взять значения:**

1. Откройте Supabase Dashboard:
   ```
   https://supabase.com/dashboard/project/bbayqzqlqgqipohulcsd/settings/api
   ```

2. Скопируйте:
   - **Project URL** → используйте для `VITE_SUPABASE_URL`
   - **anon/public key** → используйте для `VITE_SUPABASE_ANON_KEY`

3. Добавьте как Secrets:
   - Settings → Secrets and variables → Actions
   - New repository secret
   - Вставьте значения

---

### 3️⃣ Проверьте что workflow запустился:

**Откройте Actions:**
```
https://github.com/YOUR_USERNAME/YOUR_REPO/actions
```

**Должно быть:**
```
✅ 🚀 Deploy to GitHub Pages
   ├─ 🏗️ Build (в процессе или успешно)
   └─ 🚀 Deploy (ожидает или успешно)
```

---

## 🔧 Альтернативные версии Node.js (если проблема повторится):

Если ошибка с `20.x` все еще возникает, попробуйте:

### Вариант 1: LTS версия (рекомендуется)
```yaml
node-version: 'lts/*'
```

### Вариант 2: Конкретная версия
```yaml
node-version: '20.10.0'
```

### Вариант 3: Node.js 18 (более стабильная)
```yaml
node-version: '18.x'
```

### Вариант 4: Последняя доступная
```yaml
node-version: 'latest'
```

---

## ✅ Проверка после исправления:

### Шаг 1: Build должен пройти

В Actions логах вы должны увидеть:

```
🟢 Setup Node.js
  ✅ Successfully set up Node.js version 20.x
  ✅ Version: 20.10.0 (или другая из 20.x)
  
📦 Install dependencies
  ✅ npm ci completed successfully
  
🏗️ Build with environment variables
  ✅ vite build completed
  ✅ dist/ folder created
```

---

### Шаг 2: Environment variables должны работать

Если в логах вы видите:

```
⚠️ VITE_SUPABASE_URL is undefined
⚠️ VITE_SUPABASE_ANON_KEY is undefined
```

**Значит:** Secrets не добавлены или имена неправильные.

**Решение:** Проверьте точные имена в Settings → Secrets → Actions.

---

## 📋 Финальный чеклист:

- [ ] ✅ Исправлена версия Node.js (`'20.x'` вместо `'20'`)
- [ ] ✅ Добавлена секция `env:` в build шаг
- [ ] ✅ Файл находится в `/.github/workflows/deploy.yml`
- [ ] ✅ Старый файл `/workflows/deploy.yml` удален
- [ ] ✅ GitHub Secrets добавлены (VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY)
- [ ] ✅ Код запушен на GitHub
- [ ] ✅ Workflow запустился в Actions
- [ ] ✅ Build прошел успешно
- [ ] ✅ Deploy завершился

---

## 🎉 Ожидаемый результат:

**Через 2-3 минуты после push:**

1. ✅ GitHub Actions успешно соберет проект
2. ✅ Задеплоит на GitHub Pages
3. ✅ Сайт будет доступен по адресу:
   ```
   https://YOUR_USERNAME.github.io/YOUR_REPO/
   ```

---

## 🐛 Troubleshooting:

### Ошибка все еще возникает:

**Попробуйте:**
1. Измените версию на `'lts/*'`
2. Или используйте `'18.x'` (более стабильная)
3. Re-run workflow в GitHub Actions

---

### Build проходит, но сайт не работает:

**Проблема:** Скорее всего environment variables не передались.

**Решение:**
1. Проверьте логи build в Actions
2. Убедитесь что видите `env:` переменные
3. Проверьте что Secrets названы ТОЧНО как в workflow

---

## 💡 Pro Tip:

### Как проверить что environment variables работают:

В логах build должно быть примерно так:

```bash
🏗️ Build with environment variables
  
  > vite build
  
  # Vite должен видеть переменные:
  Loading environment variables...
  ✅ VITE_SUPABASE_URL: https://******.supabase.co
  ✅ VITE_SUPABASE_ANON_KEY: eyJ****... (скрыто)
  
  Building for production...
  ✅ dist/index.html created
```

Если переменные **НЕ видны** - значит Secrets не добавлены или неправильно названы!

---

## 🚀 Готово!

**Все исправлено и готово к деплою!**

**Следующие команды:**

```bash
# Push исправления
git add .github/workflows/deploy.yml
git commit -m "🔧 Fix Node.js version + add env variables"
git push origin main

# Откройте Actions
# https://github.com/YOUR_USERNAME/YOUR_REPO/actions

# Через 2-3 минуты сайт будет live! 🎉
```

---

**💜 Katia Platform - Node.js версия исправлена!**
