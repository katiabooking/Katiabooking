# ⚡ .ENV Быстрый старт

## ✅ Файл .env уже создан!

Откройте файл `/.env` в корне проекта - все основные переменные уже там!

---

## 🔧 Что нужно сделать СЕЙЧАС:

### 1. Получите Supabase Service Role Key

**Где:**  
Supabase Dashboard → Settings → API → `service_role` key (секретный)

**Вставьте в .env:**
```env
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### 2. Получите Stripe Keys (если нужны платежи)

**Где:**  
https://dashboard.stripe.com/test/apikeys

**Вставьте в .env:**
```env
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_...
```

### 3. Перезапустите dev сервер

```bash
# Остановите: Ctrl+C
npm run dev
```

---

## 🎯 Что уже работает (без дополнительных настроек):

✅ **Supabase подключение** (SUPABASE_URL и SUPABASE_ANON_KEY)  
✅ **Аутентификация пользователей**  
✅ **База данных**  
✅ **File storage**

---

## 🔐 Важно про безопасность:

### ✅ ПРАВИЛЬНО:

```env
# Frontend (с VITE_ префиксом)
VITE_SUPABASE_URL=https://...
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Backend (без VITE_)
SUPABASE_SERVICE_ROLE_KEY=...
STRIPE_SECRET_KEY=sk_test_...
```

### ❌ НЕПРАВИЛЬНО:

```typescript
// ❌ НЕ ДЕЛАЙТЕ:
const secret = "sk_live_hardcoded_secret";  // Hardcoded в коде

// ❌ НЕ ДЕЛАЙТЕ:
git add .env  // Коммит секретов в git!

// ❌ НЕ ДЕЛАЙТЕ:
const secretKey = import.meta.env.STRIPE_SECRET_KEY;  // Secret во фронтенде
```

---

## 📝 Опциональные настройки (позже):

Эти можно настроить когда понадобятся:

- **Email сервис** (Resend, SendGrid) - для email уведомлений
- **SMS сервис** (Twilio) - для SMS уведомлений
- **Push notifications** (VAPID keys) - для push уведомлений

---

## 🐛 Проблемы?

### "Cannot read environment variables"
→ Перезапустите dev сервер (Ctrl+C, затем `npm run dev`)

### ".env не работает"
→ Убедитесь что файл `.env` в корне проекта (рядом с package.json)

### "CORS error"
→ Проверьте SUPABASE_URL и SUPABASE_ANON_KEY

---

## 📚 Полная документация:

**Детальная инструкция:** [ENV_VARIABLES_GUIDE.md](./ENV_VARIABLES_GUIDE.md)

---

## ✅ Чеклист:

- [x] Файл .env создан
- [x] Базовые Supabase переменные работают
- [ ] SUPABASE_SERVICE_ROLE_KEY добавлен
- [ ] Stripe keys добавлены (если нужны)
- [ ] Dev сервер перезапущен

---

**Готово! Можете начинать разработку!** 🚀
