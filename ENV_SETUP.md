# 🔧 Environment Variables Setup

## ✅ Файл `.env` уже создан!

Файл `.env` находится в корневой директории проекта со следующими переменными:

```bash
VITE_SUPABASE_URL=https://bbayqzqlqgqipohulcsd.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_FmZxEB3IzAM-KrIbH372xQ_vMx5KJ42
```

---

## 🚀 Если всё ещё видите ошибку:

### **Шаг 1: ПЕРЕЗАПУСТИТЕ dev server**

```bash
# Остановите сервер (Ctrl+C)
# Затем запустите снова:
npm run dev
# или
pnpm dev
```

**⚠️ ВАЖНО:** Vite загружает `.env` только при запуске. Изменения требуют перезапуска!

---

### **Шаг 2: Проверьте структуру файлов**

Убедитесь что `.env` находится в **корне проекта**:

```
katia-platform/
├── .env                    ← Должен быть ЗДЕСЬ
├── .env.example
├── .gitignore
├── package.json
├── vite.config.js
├── src/
│   ├── app/
│   ├── lib/
│   │   └── supabase.ts
│   └── ...
└── ...
```

---

### **Шаг 3: Проверьте содержимое `.env`**

Откройте `.env` и убедитесь что там:

```bash
VITE_SUPABASE_URL=https://bbayqzqlqgqipohulcsd.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_FmZxEB3IzAM-KrIbH372xQ_vMx5KJ42
```

**⚠️ Обратите внимание:**
- ✅ Префикс `VITE_` обязателен для Vite
- ✅ Нет пробелов вокруг `=`
- ✅ Нет кавычек вокруг значений
- ✅ Одна переменная на строку

---

### **Шаг 4: Проверьте в браузере**

После перезапуска сервера, откройте консоль браузера (F12) и введите:

```javascript
console.log(import.meta.env.VITE_SUPABASE_URL);
```

**Должно вывести:** `https://bbayqzqlqgqipohulcsd.supabase.co`

Если выводит `undefined` - значит `.env` не загружается.

---

## 🐛 Troubleshooting

### Проблема: "Переменные всё ещё undefined"

**Решение 1:** Убедитесь что используете правильную команду:
```bash
# ✅ Правильно:
npm run dev
pnpm dev
yarn dev

# ❌ Неправильно:
node server.js
npm start
```

**Решение 2:** Очистите кеш:
```bash
rm -rf node_modules/.vite
npm run dev
```

**Решение 3:** Пересоздайте `.env`:
```bash
# Удалите старый
rm .env

# Скопируйте из примера
cp .env.example .env

# Отредактируйте и добавьте реальные значения
nano .env  # или используйте любой редактор
```

---

### Проблема: "import.meta is undefined"

Это значит что вы используете **неправильный bundler** или старую версию Node.js.

**Решение:**
```bash
# Проверьте версию Node
node -v
# Должна быть >= 18.0.0

# Обновите Node если нужно
nvm install 20
nvm use 20
```

---

### Проблема: ".env не игнорируется git"

**Решение:**
```bash
# Убедитесь что .gitignore создан:
cat .gitignore

# Должно быть:
.env
.env.local
.env.*.local

# Если уже закоммитили .env:
git rm --cached .env
git commit -m "Remove .env from git"
```

---

## 📋 Checklist для команды:

- [ ] `.env` файл создан в корне проекта
- [ ] Переменные начинаются с `VITE_`
- [ ] Dev server перезапущен после создания `.env`
- [ ] В консоли браузера `import.meta.env.VITE_SUPABASE_URL` возвращает URL
- [ ] `.env` добавлен в `.gitignore`
- [ ] `.env.example` создан для примера

---

## ✅ Всё работает?

После выполнения всех шагов:

1. Откройте `/auth`
2. Попробуйте зарегистрироваться
3. Не должно быть ошибок в консоли

**Если всё ещё не работает** - дайте знать и я помогу! 🚀
