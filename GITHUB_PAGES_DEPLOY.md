# 🚀 Инструкция по деплою на GitHub Pages

## ✅ Что было сделано

1. ✅ **Настроен vite.config.ts** с базовым путем `base: './'`
2. ✅ **Создан /public/404.html** для обработки client-side routing
3. ✅ **Добавлен скрипт редиректа в index.html** для SPA навигации
4. ✅ **Создан GitHub Actions workflow** для автоматического деплоя

---

## 📋 Пошаговая инструкция

### Шаг 1: Настройка GitHub Pages в репозитории

1. Перейдите в ваш репозиторий на GitHub
2. Откройте **Settings** → **Pages** (левая боковая панель)
3. В разделе **Source** выберите:
   - **Source**: `GitHub Actions`
   
   ![GitHub Pages Source](https://docs.github.com/assets/cb-47267/images/help/pages/publishing-source-drop-down.png)

---

### Шаг 2: Обновите vite.config.ts (ВАЖНО!)

Если ваш репозиторий называется НЕ на корневом домене, нужно указать имя репозитория:

**Сценарий А: Репозиторий с именем (например, `katia-booking`)**

URL будет: `https://username.github.io/katia-booking/`

```typescript
// vite.config.ts
export default defineConfig({
  base: '/katia-booking/',  // ← Замените на имя вашего репозитория
  // ... остальные настройки
})
```

**Сценарий Б: Пользовательский домен (например, `katia.beauty`)**

URL будет: `https://katia.beauty/`

```typescript
// vite.config.ts
export default defineConfig({
  base: '/',  // ← Оставьте корень
  // ... остальные настройки
})
```

**Сценарий В: Username.github.io репозиторий**

Если ваш репозиторий называется `username.github.io`:

```typescript
// vite.config.ts
export default defineConfig({
  base: './',  // ← Текущая настройка подходит
  // ... остальные настройки
})
```

---

### Шаг 3: Запушьте изменения

```bash
git add .
git commit -m "Configure for GitHub Pages deployment"
git push origin main
```

---

### Шаг 4: Проверьте деплой

1. Перейдите во вкладку **Actions** в вашем репозитории
2. Дождитесь завершения workflow "Deploy to GitHub Pages" (✅ зеленая галочка)
3. Откройте **Settings** → **Pages** 
4. Вы увидите URL вашего сайта (например: `https://username.github.io/repo-name/`)
5. Нажмите на URL - сайт должен работать! 🎉

---

## 🔧 Настройка пользовательского домена (опционально)

Если у вас есть свой домен (например, `katia.beauty`):

### 1. Добавьте домен в GitHub

1. **Settings** → **Pages** → **Custom domain**
2. Введите ваш домен (например: `katia.beauty`)
3. Нажмите **Save**

### 2. Настройте DNS записи у регистратора домена

Добавьте следующие записи:

**Для корневого домена (katia.beauty):**

```
Тип: A
Host: @
Value: 185.199.108.153
       185.199.109.153
       185.199.110.153
       185.199.111.153
```

**Для www поддомена:**

```
Тип: CNAME
Host: www
Value: username.github.io
```

### 3. Обновите vite.config.ts

```typescript
export default defineConfig({
  base: '/',  // ← Для пользовательского домена используйте корень
  // ...
})
```

---

## 🐛 Устранение проблем

### Проблема: Страница показывает 404

**Решение:**
1. Проверьте правильность `base` в `vite.config.ts`
2. Убедитесь, что в **Settings → Pages → Source** выбрано `GitHub Actions`
3. Проверьте, что workflow выполнился успешно (вкладка Actions)

### Проблема: CSS/JS файлы не загружаются

**Решение:**
1. Проверьте `base` в `vite.config.ts` - должен соответствовать имени репозитория
2. Пересоберите и запушьте:
   ```bash
   npm run build
   git add dist
   git commit -m "Rebuild with correct base path"
   git push
   ```

### Проблема: React Router не работает (404 на подстраницах)

**Решение:**
Уже исправлено! Файлы `/public/404.html` и скрипт в `index.html` обрабатывают client-side routing.

---

## 📝 Альтернативный метод: Ручной деплой

Если GitHub Actions не работает, можно задеплоить вручную:

```bash
# 1. Соберите проект
npm run build

# 2. Перейдите в папку dist
cd dist

# 3. Инициализируйте git
git init
git add -A
git commit -m "Deploy"

# 4. Запушьте в gh-pages branch
git push -f git@github.com:username/repo-name.git main:gh-pages

# 5. Вернитесь обратно
cd ..
```

Затем в **Settings → Pages** выберите:
- **Source**: `Deploy from a branch`
- **Branch**: `gh-pages` / `root`

---

## ✨ Проверка работы

После успешного деплоя проверьте:

- ✅ Главная страница загружается
- ✅ Навигация работает (страницы не показывают 404)
- ✅ CSS стили применяются
- ✅ Изображения загружаются
- ✅ React Router переходы работают

---

## 🎯 Следующие шаги

После успешного деплоя на GitHub Pages:

1. **Настройте HTTPS** (GitHub Pages автоматически выдает SSL сертификат)
2. **Добавьте Google Analytics** для отслеживания трафика
3. **Настройте SEO** - robots.txt и sitemap.xml уже добавлены
4. **Мониторинг** - используйте GitHub Actions для уведомлений о деплое

---

## 📞 Нужна помощь?

Если что-то не работает:

1. Проверьте логи в **Actions** вкладке
2. Убедитесь, что `base` в `vite.config.ts` правильный
3. Очистите кеш браузера (Ctrl+Shift+R)
4. Проверьте Console в DevTools на ошибки

---

**Статус:** ✅ Проект готов к деплою на GitHub Pages!

**URL после деплоя:** `https://[ваш-username].github.io/[имя-репозитория]/`
