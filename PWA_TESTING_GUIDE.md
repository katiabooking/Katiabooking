# 🧪 PWA TESTING GUIDE - Katia Beauty Platform

## 🚀 БЫСТРЫЙ СТАРТ ТЕСТИРОВАНИЯ

### **1. Запустите приложение:**

```bash
npm run dev
# или
yarn dev
```

Откройте: `http://localhost:5173`

---

## 📱 ТЕСТИРОВАНИЕ НА РАЗНЫХ УСТРОЙСТВАХ

### **Desktop Chrome/Edge:**

#### A. Проверка Service Worker:
1. `F12` → `Application` tab
2. `Service Workers` → Должен быть зарегистрирован ✅
3. Статус: `activated and is running`

#### B. Проверка Manifest:
1. `F12` → `Application` → `Manifest`
2. Проверить:
   - ✅ Name: "Katia Beauty Platform"
   - ✅ Icons: 8 иконок (72px - 512px)
   - ✅ Theme color: #9333ea
   - ✅ Display: standalone

#### C. Lighthouse Audit:
1. `F12` → `Lighthouse` tab
2. Категории: `Performance`, `PWA`, `Best Practices`
3. Нажать `Generate report`
4. **Цель:** PWA score 90+ ✅

#### D. Установка приложения:
1. В адресной строке справа → иконка установки (⊕)
2. Или баннер внизу справа → "Установить"
3. Нажать `Install`
4. Проверить: иконка появилась на рабочем столе

#### E. Offline тест:
1. `F12` → `Network` tab
2. Переключить в `Offline` режим
3. Обновить страницу (`Ctrl+R`)
4. **Ожидается:** Показывается `/offline.html` с красивым дизайном

---

### **Mobile Android (Chrome):**

#### A. Через USB Debugging:
1. Включить Developer Mode на телефоне
2. Settings → Developer Options → USB Debugging
3. Подключить к компьютеру
4. Chrome Desktop → `chrome://inspect#devices`
5. Открыть Katia на телефоне → Inspect

#### B. Установка PWA:
1. Открыть katia.com на телефоне
2. Появится баннер "Установить приложение"
3. Нажать `Установить`
4. Иконка на главном экране
5. Запустить → работает как приложение

#### C. Push Notifications:
1. В приложении → Settings → PWA Settings
2. Включить "Уведомления"
3. Разрешить в системном диалоге
4. Закрыть приложение
5. Отправить тестовое уведомление (через backend)

---

### **iOS Safari:**

#### A. Установка на главный экран:
1. Открыть katia.com в Safari
2. Нажать кнопку "Поделиться" (⬆️)
3. Прокрутить → "На экран Домой"
4. Нажать "Добавить"
5. Иконка Katia на главном экране

#### B. Проверка Standalone:
1. Запустить с главного экрана
2. Открывается в полноэкранном режиме (без Safari UI)
3. Console: `✅ Katia запущена как установленное PWA приложение`

**⚠️ Примечание:** Push notifications НЕ работают в Safari (iOS ограничение)

---

## 🧰 CHROME DEVTOOLS - ДЕТАЛЬНОЕ ТЕСТИРОВАНИЕ

### **Application Tab:**

#### **Service Workers:**
```
✅ Status: activated and is running
✅ Source: service-worker.js
✅ Scope: /
✅ Update on reload: Включить для разработки
```

**Тесты:**
- [x] Клик `Unregister` → Перезагрузка → Автоматически зарегистрируется снова
- [x] Клик `Update` → Проверка новой версии
- [x] Offline → Приложение работает

#### **Manifest:**
```
✅ Identity:
   Name: Katia Beauty Platform
   Short name: Katia
   Start URL: /

✅ Presentation:
   Display: standalone
   Orientation: portrait-primary
   Theme color: #9333ea
   Background: #ffffff

✅ Icons: 8 icons
   - icon-72x72.png
   - icon-96x96.png
   - icon-128x128.png
   - icon-144x144.png
   - icon-152x152.png
   - icon-192x192.png
   - icon-384x384.png
   - icon-512x512.png

✅ Protocol handlers: none
✅ Note taking: not specified
```

#### **Cache Storage:**

Должно быть 3 кеша:
```
katia-v1.0.0-static     (~2-5 MB)
  - /
  - /offline.html
  - /manifest.json
  - /icons/...

katia-v1.0.0-dynamic    (~5-10 MB)
  - API responses
  - HTML pages
  - JSON данные

katia-v1.0.0-images     (~10-20 MB)
  - Изображения салонов
  - Фото мастеров
  - Service photos
```

**Тесты:**
- [x] Правый клик на кеше → `Delete` → Проверка пересоздания
- [x] Offline режим → Данные берутся из кеша
- [x] Просмотр размера через PWA Settings компонент

#### **Background Services:**

**Background Sync:**
```
✅ Зарегистрированные sync tags:
   - sync-bookings (для синхронизации офлайн бронирований)
```

**Notifications:**
```
✅ Permission: granted / denied / default
✅ Push subscription: active / none
```

---

## 📊 LIGHTHOUSE ТЕСТИРОВАНИЕ

### **Как запустить:**

1. `F12` → `Lighthouse`
2. Категории:
   - ✅ Performance
   - ✅ Accessibility
   - ✅ Best Practices
   - ✅ SEO
   - ✅ PWA
3. Device: `Mobile` или `Desktop`
4. `Generate report`

### **Ожидаемые результаты:**

#### **PWA (должно быть 90+):**
```
✅ Installable:
   - Registers service worker
   - Responds with 200 when offline
   - Has manifest
   - Valid manifest
   - Icons (192px and 512px)

✅ PWA Optimized:
   - Configured for custom splash screen
   - Sets theme color
   - Content sized correctly
   - Has viewport meta tag
   - Apple touch icon
```

#### **Performance (цель 80+):**
```
✅ First Contentful Paint: < 2s
✅ Largest Contentful Paint: < 3s
✅ Speed Index: < 4s
✅ Time to Interactive: < 5s
✅ Total Blocking Time: < 300ms
✅ Cumulative Layout Shift: < 0.1
```

#### **Best Practices (цель 90+):**
```
✅ Uses HTTPS (в production)
✅ No browser errors
✅ Images with correct aspect ratio
✅ Valid source maps
```

#### **SEO (цель 90+):**
```
✅ Has meta description
✅ Page has title
✅ robots.txt valid
✅ Images have alt attributes
✅ Links are crawlable
```

---

## 🔬 ФУНКЦИОНАЛЬНОЕ ТЕСТИРОВАНИЕ

### **Offline Mode:**

**Тест 1: Базовая офлайн работа**
```
1. Загрузить katia.com
2. DevTools → Network → Offline
3. Обновить страницу
4. ✅ Показывается /offline.html
5. ✅ Красивая страница с градиентом
6. ✅ Кнопка "Повторить попытку"
```

**Тест 2: Кеширование данных**
```
1. Открыть /owner (Owner Dashboard)
2. Подождать загрузку данных
3. DevTools → Network → Offline
4. Обновить страницу
5. ✅ Страница загружается из кеша
6. ✅ Данные показываются (старые)
7. ✅ Индикатор "Нет подключения" вверху
```

**Тест 3: Восстановление подключения**
```
1. В offline режиме
2. DevTools → Network → Online
3. ✅ Индикатор меняется на "Подключено"
4. ✅ Зеленое уведомление "Подключение восстановлено"
5. ✅ Автоматическая синхронизация данных
```

### **Install Flow:**

**Desktop:**
```
1. Открыть katia.com
2. Подождать 2-3 секунды
3. ✅ Появляется PWA Install Banner (правый нижний угол)
4. ✅ Иконка в адресной строке
5. Нажать "Установить" на баннере
6. ✅ Системный диалог установки
7. Подтвердить
8. ✅ Иконка на рабочем столе
9. Запустить
10. ✅ Открывается в отдельном окне
```

**Mobile Android:**
```
1. Открыть katia.com
2. Подождать 3 секунды
3. ✅ PWA Install Banner появляется
4. Нажать "Установить"
5. ✅ Системный диалог Android
6. Подтвердить
7. ✅ Иконка на главном экране
8. Запустить
9. ✅ Splash screen с логотипом
10. ✅ Работает как нативное приложение
```

**iOS Safari:**
```
1. Открыть katia.com
2. ✅ Баннер с инструкциями (iOS)
3. Нажать Share (⬆️)
4. Выбрать "На экран Домой"
5. ✅ Предпросмотр с иконкой
6. Нажать "Добавить"
7. ✅ Иконка на главном экране
8. Запустить
9. ✅ Полноэкранный режим
```

### **Connection Status:**

**Тест 1: Индикатор offline**
```
1. Открыть приложение
2. DevTools → Network → Offline
3. ✅ Красный баннер вверху экрана
4. ✅ Текст: "Нет подключения к интернету • Работаем в offline режиме"
5. ✅ Иконка WifiOff
```

**Тест 2: Уведомление о восстановлении**
```
1. В offline режиме
2. DevTools → Network → Online
3. ✅ Зеленое уведомление справа
4. ✅ Текст: "Подключение восстановлено"
5. ✅ Автоматически исчезает через 5 секунд
```

### **PWA Settings:**

**Путь:** Owner Dashboard → Settings → PWA Settings

**Тест 1: Статус приложения**
```
1. Открыть PWA Settings
2. ✅ Карточка "Статус приложения"
3. ✅ Показывает: Установлено / Не установлено
4. ✅ Показывает платформу: iOS / Android / Desktop
5. ✅ Кнопка "Установить" (если не установлено)
```

**Тест 2: Уведомления**
```
1. Секция "Push-уведомления"
2. Включить переключатель
3. ✅ Системный запрос разрешения
4. Разрешить
5. ✅ Переключатель активен
6. ✅ Toast: "Уведомления включены"
7. Выключить переключатель
8. ✅ Toast: "Уведомления отключены"
```

**Тест 3: Управление кешем**
```
1. Секция "Offline режим"
2. ✅ Показывает размер кеша (например, "15.3 MB")
3. Нажать "Очистить кеш"
4. ✅ Confirm диалог
5. Подтвердить
6. ✅ Toast: "Кеш очищен"
7. ✅ Размер обновился (близко к 0)
```

---

## 🐛 ИЗВЕСТНЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

### **Проблема 1: Service Worker не регистрируется**

**Симптомы:**
- В консоли ошибка: "Failed to register service worker"
- Application → Service Workers пусто

**Решения:**
1. Проверить HTTPS (SW работает только на HTTPS или localhost)
2. Проверить путь к файлу: `/public/service-worker.js` должен быть доступен
3. Очистить кеш браузера: `Ctrl+Shift+Delete`
4. Hard refresh: `Ctrl+Shift+R`

### **Проблема 2: Manifest не загружается**

**Симптомы:**
- Application → Manifest показывает ошибку
- Install prompt не появляется

**Решения:**
1. Проверить `<link rel="manifest" href="/manifest.json">` в index.html
2. Проверить JSON синтаксис manifest.json
3. Проверить MIME type: должен быть `application/manifest+json`
4. Проверить CORS headers

### **Проблема 3: Иконки не отображаются**

**Симптомы:**
- Install dialog показывает дефолтную иконку
- Application → Manifest: ошибки для иконок

**Решения:**
1. Генерировать иконки через `/public/icons/icon-generator.html`
2. Проверить размеры: 72, 96, 128, 144, 152, 192, 384, 512
3. Проверить формат: PNG
4. Проверить пути в manifest.json

### **Проблема 4: Offline page не показывается**

**Симптомы:**
- При offline показывается динозаврик Chrome
- Нет красивой offline страницы

**Решения:**
1. Проверить precache в service-worker.js:
   ```javascript
   const PRECACHE_URLS = [
     '/',
     '/offline.html',  // ← Должно быть!
     ...
   ]
   ```
2. Unregister SW → Перезагрузить → Зарегистрировать заново
3. Проверить файл `/public/offline.html` существует

### **Проблема 5: Push notifications не работают**

**Симптомы:**
- При включении уведомлений ошибка
- Subscription не создается

**Решения:**
1. Проверить HTTPS (обязательно!)
2. В `pwaUtils.ts` заменить `YOUR_VAPID_PUBLIC_KEY` на реальный
3. Генерация VAPID keys:
   ```bash
   npx web-push generate-vapid-keys
   ```
4. iOS Safari: Push НЕ поддерживается (ограничение Apple)

---

## ✅ ЧЕКЛИСТ ФИНАЛЬНОГО ТЕСТИРОВАНИЯ

### **Desktop:**
- [ ] Service Worker зарегистрирован
- [ ] Manifest загружен корректно
- [ ] Lighthouse PWA score 90+
- [ ] Install prompt появляется
- [ ] Установка работает
- [ ] Offline page показывается
- [ ] Кеширование работает
- [ ] Connection status отображается
- [ ] PWA Settings функционирует

### **Android:**
- [ ] Install banner появляется
- [ ] Установка на главный экран работает
- [ ] Splash screen показывается
- [ ] Иконка корректная
- [ ] Standalone режим работает
- [ ] Push notifications (после backend)
- [ ] Offline режим

### **iOS:**
- [ ] Инструкции по установке показываются
- [ ] Add to Home Screen работает
- [ ] Иконка корректная
- [ ] Standalone режим работает
- [ ] Offline режим
- [ ] ⚠️ Push НЕ работают (ожидаемо)

### **Performance:**
- [ ] First load < 3s
- [ ] Cached load < 1s
- [ ] Lighthouse Performance 80+
- [ ] No console errors
- [ ] Images optimized

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

После успешного тестирования PWA:

1. **Mobile-First Адаптация** (Phase 2)
   - Responsive design
   - Touch-friendly UI
   - Bottom navigation
   - Swipe gestures

2. **Push Notifications Backend** (Phase 3)
   - VAPID keys setup
   - Backend API для push
   - Notification triggers
   - User preferences

3. **Advanced Features** (Phase 4)
   - Background sync для офлайн изменений
   - File handling
   - Badging API
   - Shortcuts API

---

**Готово к тестированию!** 🚀
