# 🎯 Руководство: Система Управления Ресурсами для Katia Platform

## 📋 Обзор Системы

Платформа Katia теперь включает комплексную систему управления рабочими местами и ресурсами, которая предотвращает двойное бронирование и оптимизирует использование оборудования салона.

## 🔧 Новые Компоненты

### 1. **ResourceManagementTab.tsx**
Полноценная панель управления ресурсами с функциями:
- ✅ Добавление/редактирование/удаление ресурсов
- ✅ Привязка услуг к типам ресурсов
- ✅ Управление количеством рабочих мест
- ✅ Статусы: Active, Maintenance, Inactive
- ✅ Статистика использования ресурсов

**Доступ:** Только для Owner (владельца салона)

**Расположение:** Settings → Resource Management

### 2. **resourceAvailability.ts**
Утилита для проверки доступности ресурсов с функциями:
- `checkResourceAvailability()` - проверка доступности на конкретное время
- `getAvailableTimeSlots()` - получение всех доступных слотов
- `validateBooking()` - валидация бронирования
- `suggestAlternativeSlots()` - предложение альтернативных времен
- `getResourceUtilization()` - статистика загрузки ресурсов

### 3. **WaitingListTab.tsx** (Обновлено)
Теперь полностью интегрирован с системой бронирования:
- ✅ Все кнопки работают (Notify, Book, Confirm, Cancel)
- ✅ Модальное окно отправки уведомлений
- ✅ Интеграция с календарем через callback `onBookFromWaitingList`
- ✅ Обновление статусов в реальном времени

## 🏗️ Архитектура Интеграции

```
┌─────────────────────────────────────────────────────────┐
│                    Salon Dashboard                       │
└─────────────────────────────────────────────────────────┘
                            │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        ▼                  ▼                  ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│   Calendar   │  │ Waiting List │  │   Settings   │
│     Tab      │  │     Tab      │  │     Tab      │
└──────────────┘  └──────────────┘  └──────────────┘
        │                  │                  │
        │                  │                  ▼
        │                  │         ┌──────────────┐
        │                  │         │  Resource    │
        │                  │         │  Management  │
        │                  │         └──────────────┘
        │                  │                  │
        └──────────────────┴──────────────────┘
                            │
                            ▼
                ┌────────────────────────┐
                │  Resource Availability │
                │       Utility          │
                └────────────────────────┘
```

## 💡 Как Работает Система

### Пример: Педикюрные Кресла

```typescript
// 1. Владелец создает ресурс в Settings
{
  name: "Pedicure Chairs",
  category: "Nails",
  quantity: 2,  // У нас 2 педикюрных кресла
  assignedServices: ["Pedicure", "Spa Pedicure"],
  status: "active"
}

// 2. При попытке бронирования система проверяет
const availability = checkResourceAvailability(
  resources,
  existingBookings,
  "Pedicure",        // Услуга
  "2024-12-25",      // Дата
  "14:00",           // Время
  60                 // Длительность (минуты)
);

// 3. Результат проверки
{
  isAvailable: true,          // Можно бронировать
  availableSlots: 1,          // Осталось 1 свободное место
  totalCapacity: 2,           // Всего 2 места
  message: "1 of 2 slots available"
}
```

### Сценарий Предотвращения Overbooking

**Ситуация:**
- Салон имеет 2 педикюрных кресла
- Уже забронировано 2 педикюра на 14:00
- Клиент пытается забронировать еще один на 14:00

**Результат:**
```typescript
{
  isAvailable: false,
  availableSlots: 0,
  totalCapacity: 2,
  errors: ["No available resources for Pedicure at 14:00. All 2 slots are booked."]
}
```

**Альтернативы:**
```typescript
suggestAlternativeSlots(...)
// → ["13:30", "14:30", "15:00", "15:30", "16:00"]
```

## 🔗 Интеграция Waiting List с Calendar

### Поток Работы

1. **Клиент добавлен в Waiting List**
   - Заполняется форма с предпочтениями
   - Entry создается со статусом `waiting`

2. **Slot освобождается**
   - Система автоматически находит подходящие записи в Waiting List
   - Или администратор вручную нажимает "Notify"

3. **Отправка уведомления**
   - Открывается модальное окно с предзаполненным сообщением
   - Отправляется SMS + Email + WhatsApp
   - Статус меняется на `notified`

4. **Клиент подтверждает**
   - Администратор нажимает "Book"
   - Callback передает данные в Calendar
   - Создается реальное бронирование с проверкой ресурсов
   - Статус меняется на `booked`

## 🎨 UI/UX Особенности

### Resource Management
- **Цветовое кодирование статусов:**
  - 🟢 Green: Active
  - 🟡 Yellow: Maintenance
  - ⚪ Gray: Inactive

- **Удобная таблица:**
  - Быстрое редактирование
  - Просмотр привязанных услуг
  - Статистика по категориям

### Waiting List
- **Priority Badges:**
  - 🔴 High Priority (красный)
  - ⚪ Normal (серый)
  - ⚪ Low (светло-серый)

- **Status Indicators:**
  - 🟡 Waiting (желтый)
  - 🔵 Notified (синий)
  - 🟢 Booked (зеленый)
  - 🔴 Cancelled (красный)

## 📊 Типичные Ресурсы Салона

```typescript
const typicalResources = [
  // Nails
  { name: "Manicure Tables", quantity: 3 },
  { name: "Pedicure Chairs", quantity: 2 },
  
  // Hair
  { name: "Hair Styling Stations", quantity: 4 },
  { name: "Hair Washing Basins", quantity: 2 },
  
  // Eyelashes
  { name: "Eyelash Extension Beds", quantity: 1 },
  
  // Massage & Spa
  { name: "Massage Tables", quantity: 1 },
  { name: "Facial Treatment Rooms", quantity: 2 },
  
  // Other
  { name: "Makeup Stations", quantity: 2 },
  { name: "Waxing Rooms", quantity: 1 }
];
```

## 🚀 Дальнейшие Улучшения

### Текущая Реализация (✅ Готово)
- ✅ CRUD операции для ресурсов
- ✅ Привязка услуг к ресурсам
- ✅ Проверка доступности
- ✅ Waiting List интеграция
- ✅ Все кнопки работают

### Будущие Фичи (⏳ В Планах)
- ⏳ Автоматические уведомления при освобождении слотов
- ⏳ AI-powered оптимизация расписания
- ⏳ Приоритетная очередь для VIP клиентов
- ⏳ Интеграция с внешними календарями (Google Calendar, iCal)
- ⏳ Мобильное приложение для мастеров
- ⏳ Real-time обновления через WebSocket
- ⏳ Аналитика использования ресурсов

## 🔐 Права Доступа

| Роль | Resource Management | Waiting List | Booking |
|------|-------------------|--------------|---------|
| **Owner** | ✅ Полный доступ | ✅ Полный доступ | ✅ Полный доступ |
| **Admin** | ❌ Только просмотр | ✅ Управление без удаления | ✅ Просмотр + создание |
| **Master** | ❌ Нет доступа | ⚠️ Только свои | ⚠️ Только свои |

## 💻 Примеры Использования

### Проверка доступности перед бронированием

```typescript
import { checkResourceAvailability, validateBooking } from './utils/resourceAvailability';

// Перед созданием бронирования
const validation = validateBooking(
  resources,
  existingBookings,
  {
    service: "Manicure",
    date: "2024-12-25",
    startTime: "14:00",
    duration: 60
  }
);

if (!validation.isValid) {
  // Показать ошибки
  toast.error(validation.errors[0]);
  
  // Предложить альтернативы
  const alternatives = suggestAlternativeSlots(...);
  showAlternativeSlotsModal(alternatives);
} else {
  // Создать бронирование
  createBooking(...);
  
  // Показать предупреждения если есть
  if (validation.warnings.length > 0) {
    toast.warning(validation.warnings[0]);
  }
}
```

### Получение доступных слотов для онлайн бронирования

```typescript
import { getAvailableTimeSlots } from './utils/resourceAvailability';

// На публичной странице салона
const availableSlots = getAvailableTimeSlots(
  resources,
  existingBookings,
  selectedService,    // "Pedicure"
  selectedDate,       // "2024-12-25"
  { start: "09:00", end: "20:00" },  // Рабочие часы
  30                  // Интервал слотов в минутах
);

// Отображаем только доступные времена
availableSlots.forEach(time => {
  renderTimeSlot(time); // ["09:00", "09:30", "10:00", ...]
});
```

### Статистика загрузки ресурсов

```typescript
import { getResourceUtilization } from './utils/resourceAvailability';

// В Advanced Analytics
const utilization = getResourceUtilization(
  resources,
  bookingsThisMonth,
  "2024-12-01",
  "2024-12-31"
);

// Визуализация
utilization.forEach(stat => {
  console.log(`${stat.resourceName}: ${stat.utilizationRate}%`);
  // Manicure Tables: 85%
  // Pedicure Chairs: 92%
  // Massage Tables: 45%
});
```

## 🎯 Ключевые Преимущества

1. **Предотвращение Double Booking** 🚫
   - Автоматическая проверка перед каждым бронированием
   - Реальное отображение доступности

2. **Оптимизация Ресурсов** 📈
   - Понимание какие ресурсы используются чаще
   - Планирование расширения на основе данных

3. **Улучшенный UX** ✨
   - Клиенты видят только реально доступные слоты
   - Автоматические предложения альтернатив

4. **Масштабируемость** 🏢
   - Поддержка мультисалонности
   - Каждый салон имеет свою конфигурацию ресурсов

## 📞 Поддержка

Для вопросов или предложений по улучшению системы управления ресурсами, обращайтесь к документации API или создавайте issue в репозитории проекта.

---

**Создано для Katia Platform** 💜
Современная SaaS платформа для салонов красоты
